import { Meta, Story, Canvas } from '@storybook/addon-docs';
import LinkExamples from '../doc-helper/LinkExamples.vue';

<Meta title="Styleguide/Links" component={LinkExamples} />

# Links

- Zwei Varianten
- Icons optional
- Pfeil icon hinten platziert
- Flexibles Icon vorne platziert
- Variante je nach Einsatz wählbar

<Canvas>
  <Story name="Demo">
      {()=>{
        return {
          components: { LinkExamples },
          template: `
            <LinkExamples />
            `,
        };
      }}
    </Story>
</Canvas>

Keine eigene Komponente. Links werden als [SCSS Styles mit ausgeliefert](src/assets/styles/links.scss).

<Story name="Usage"></Story>

## Beispiel Implementierung in der App
```css
a:any-link {
  @include text-links();
}
```

## Beispiel Implementierung in Komponenten
```css
.link {
  @include text-links();
}

.decent {
  @include text-links-decent();
  .demo--dark & {
    @include text-links-decent(dark);
  }
}
```
