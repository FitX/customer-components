import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import IconClose from '@/assets/icons/close.svg?raw';
import IconClear from '@/assets/icons/clear.svg?raw';
import IconBack from '@/assets/icons/back.svg?raw';

export const Icons = [IconClose, IconClear, IconBack];

export const getIcon = (raw) => <div dangerouslySetInnerHTML={{ __html: raw }}></div>;

export const RenderIcons = () => (
  <>
    {Icons.map((item) => (
      <IconItem
        name="demo"
        key={item}>
        {getIcon(item)}
      </IconItem>
    ))}
  </>
);

<Meta title="Icons" />

## Übersicht Icons

<IconGallery>
  <IconItem name="Close">{getIcon(IconClose)}</IconItem>
  <IconItem name="Clear">{getIcon(IconClear)}</IconItem>
  <IconItem name="Back">{getIcon(IconBack)}</IconItem>
</IconGallery>

<style>
  {`
  * {

`}
</style>
