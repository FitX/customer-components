# useInput

This composable provides an easy way to manage loading and error states.
It allows you to start and stop loading processes and to store errors that occur during loading.
The composable can be combined with the component [fitx-loading](?path=/docs/components-form-input--docs).

## Usage

### Import and Initialize

```ts
import { useInput } from '@fitx/customer-components';

@TODO
```

### Properties and Methods

#### Options
- @TODO

#### Return Values
- @TODO

### Example

```html
<!-- some-component.vue -->
<script lang="ts" setup>
import { computed } from 'vue';
import { useInput } from '@fitx/customer-components';
```
